/**
 * InstallPromptBanner — Deactivated in production.
 *
 * Campus Connect no longer promotes PWA installation prompts,
 * banners, or manual "Add to Home Screen" instructions.
 * Technical PWA capabilities (service worker, offline caching, manifest)
 * remain fully operational.
 */
export default function InstallPromptBanner() {
  return null;
}
